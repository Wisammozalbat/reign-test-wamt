import React from 'react';
import Tabs from '../../components/tabs/Tabs';
import { MainContainer } from './MainStyle';

const Main = () => {
  return (
    <MainContainer>
      <Tabs />
    </MainContainer>
  );
};

export default Main;
